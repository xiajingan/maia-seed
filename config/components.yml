version: 1
components:
  maia-seed:
    type: python-package
    source: src/seed
    artifacts:
      - dist/*.whl
      - dist/*.whl.sig
      - dist/*.whl.sbom.json
      - dist/*.whl.provenance.json
    contracts: []
