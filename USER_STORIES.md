# Seed 用户故事

> 本文件是产品需求的唯一输入真源，定义谁在什么场景下遇到什么问题、希望得到什么可观察结果。PRD 细化产品行为，技术方案设计实现；除已确认外部约束外，本文件不预定页面、组件、API、数据库或技术选型。

> 迁移说明：新 `US-###` 是本仓库的 Story 主键；历史 ID 保留在索引和“来源 / 证据”中，继续用于跨项目与历史文档追溯。

## 产品愿景

根据真实消费者需求提供无业务语义、可验证、可签名和可迁移的 Python 公共能力，并以不可变 Delivery 供消费工程精确锁定。

## 产品策略与体验原则

| ID | 原则 | 适用场景 | 明确代价或不适用条件 |
|----|------|----------|----------------------|
| PS-001 | 消费者驱动而非独立扩建 | 新增公共能力与适配器 | 无 accepted Assignment 时不承诺交付 |
| PS-002 | 保持无业务语义 | 状态机、权限算法、客户端与运行基础 | 领域模型留在所有者工程 |
| PS-003 | Build Once 与精确摘要 | wheel 构建、Test 与 release | 禁止 latest、Git/path 依赖和二次构建 |
| PS-004 | 破坏变化必须迁移与删除 | 公共 API 与跨仓库升级 | 采用 add→migrate→remove |

### 迁移保留的项目边界与路线

> 本文件是 Seed 的公共能力输入真源，不是独立产品路线。外部工程通过统一 `dependency` Assignment 提出需求；Seed 自主接收、映射本地 Story、规划并执行 Sprint，再以 Delivery 更新交付状态。

能力成熟度为 S0 kernel/contracts → S1 framework/middleware adapters → S2 reliable client → S3 compatibility kit。Assignment 仅在 Seed 主动规划时进入本文件和 Sprint；`pending` 不等于已承诺，`planned` 必须关联本地 Story/Sprint，`delivered` 必须存在含精确 version + SHA-256 的 `dependency-package` Delivery。每个故事都必须有纯核心测试、适用的 adapter 测试、Secret canary 和消费者契约证据。

### 产品化细化故事

### Assignment 输入与迭代归属

- `docs/assignments/inbox/` 是 Seed 的统一外部输入池；Assignment 本体不可修改，Seed 独占响应和 Delivery 状态。
- `seed-dep-mud-p0-002-secrets` 是由 Mud Secret/APPManager Story `MUD-P0-002` 在迁移后补充的 `SEED-013` 真实输入，当前仅为 `pending`；现有源码不等于已纳入 Sprint 或可交付，必须由 Seed 后续主动接受并映射本地 Story/Sprint。
- Seed 只在用户主动规划时处理输入；接受后映射本地 Story/Sprint，不能由源工程远程启动。
- 通用维护或安全修复可由已知消费者 Assignment 或 Seed 本地 Maintenance Story 触发；必须登记影响面和迁移验证。
- Celt、Vine、Sage 不依赖 Seed；终端、Bridge、前端契约由各协议/服务所有者维护。
- 每个 `delivered` 结果必须先有有效 accepted Response，并绑定 Seed Sprint、源码提交、`dependency-package` Artifact、version + SHA-256、实际 extras、契约测试、升级与回滚结果；配置的 verifier 必须真实验证签名、SBOM 与 Build Once 并生成绑定 Delivery/Artifact 身份的受管 receipt。

## 用户画像

| 画像 | 使用上下文 | 核心诉求 | 能力或限制 |
|------|------------|----------|------------|
| 服务开发者 | 在 Seed 所负责的业务边界内完成相关工作 | 有统一配置模型；发布和消费版本化事件；可靠调用其他服务等 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 开发者 | 在 Seed 所负责的业务边界内完成相关工作 | 传播身份与租户上下文；关联上下文不会泄漏到错误异步任务 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 消费者 | 在 Seed 所负责的业务边界内完成相关工作 | 统一理解错误 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 安全负责人 | 在 Seed 所负责的业务边界内完成相关工作 | 共享认证、加密、脱敏与重放防护端口；自动发现日志中的秘密 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 审计人员 | 在 Seed 所负责的业务边界内完成相关工作 | 关键操作采用统一审计契约 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 运维人员 | 在 Seed 所负责的业务边界内完成相关工作 | 统一日志、指标和 Trace；依赖故障反映到正确探针 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 云原生服务 | 在 Seed 所负责的业务边界内完成相关工作 | 提供健康探针和优雅停机 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 发布负责人 | 在 Seed 所负责的业务边界内完成相关工作 | 安全发布 Seed wheel；跨仓库升级可迁移和回滚 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 架构负责人 | 在 Seed 所负责的业务边界内完成相关工作 | Seed 保持无业务语义 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 领域工程 | 在 Seed 所负责的业务边界内完成相关工作 | 用通用原语定义自己的状态机 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 调用方 | 在 Seed 所负责的业务边界内完成相关工作 | 未知写结果进入对账而非盲重试 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 维护者 | 在 Seed 所负责的业务边界内完成相关工作 | 公开 API 变化可治理 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 资源服务开发者 | 在 Seed 所负责的业务边界内完成相关工作 | 用唯一无状态算法合并权限事实 | 仅能使用已授权、版本兼容且可追溯的能力 |
| Python 服务开发者 | 在 Seed 所负责的业务边界内完成相关工作 | 复用可替换的 Redis 访问基础；复用 OceanBase 连接基础 | 仅能使用已授权、版本兼容且可追溯的能力 |
| 依赖需求提出者 | 在 Seed 所负责的业务边界内完成相关工作 | 异步请求 Seed 公共能力 | 仅能使用已授权、版本兼容且可追溯的能力 |

## 业务术语

| 术语 | 项目内唯一含义 | 易混淆概念 |
|------|----------------|------------|
| dependency Assignment | 消费工程的不可变公共能力需求 | 远程启动 |
| Response | Seed 对输入的接纳决定 | Delivery |
| dependency-package Delivery | 绑定版本、摘要与证据的交付声明 | 源码存在 |

## Story 索引

| ID | 旧 ID | 标题 | 阶段 / 分组 | 优先级 | 状态 |
|----|-------|------|-------------|--------|------|
| US-001 | `SEED-001` | 有统一配置模型 | 核心故事 | P0 | `draft` |
| US-002 | `SEED-002` | 传播身份与租户上下文 | 核心故事 | P0 | `draft` |
| US-003 | `SEED-003` | 统一理解错误 | 核心故事 | P0 | `draft` |
| US-004 | `SEED-004` | 发布和消费版本化事件 | 核心故事 | P0 | `draft` |
| US-005 | `SEED-005` | 共享认证、加密、脱敏与重放防护端口 | 核心故事 | P0 | `draft` |
| US-006 | `SEED-006` | 关键操作采用统一审计契约 | 核心故事 | P0 | `draft` |
| US-007 | `SEED-007` | 统一日志、指标和 Trace | 核心故事 | P0 | `draft` |
| US-008 | `SEED-008` | 提供健康探针和优雅停机 | 核心故事 | P0 | `draft` |
| US-009 | `SEED-009` | 可靠调用其他服务 | 核心故事 | P0 | `draft` |
| US-010 | `SEED-010` | 安全发布 Seed wheel | 核心故事 | P0 | `done` |
| US-011 | `SEED-011` | Seed 保持无业务语义 | 核心故事 | P0 | `draft` |
| US-012 | `SEED-012` | 用通用原语定义自己的状态机 | 核心故事 | P0 | `draft` |
| US-013 | `SEED-013` | 只在调用点解析通用 Secret | 核心故事 | P0 | `draft` |
| US-014 | `SEED-014` | 关联上下文不会泄漏到错误异步任务 | 产品化细化故事 | P0 | `draft` |
| US-015 | `SEED-015` | 未知写结果进入对账而非盲重试 | 产品化细化故事 | P0 | `draft` |
| US-016 | `SEED-016` | 依赖故障反映到正确探针 | 产品化细化故事 | P0 | `draft` |
| US-017 | `SEED-017` | 公开 API 变化可治理 | 产品化细化故事 | P0 | `draft` |
| US-018 | `SEED-018` | 自动发现日志中的秘密 | 产品化细化故事 | P0 | `draft` |
| US-019 | `SEED-019` | 用唯一无状态算法合并权限事实 | 产品化细化故事 | P0 | `draft` |
| US-020 | `SEED-020` | 复用可替换的 Redis 访问基础 | 产品化细化故事 | P0 | `draft` |
| US-021 | `SEED-021` | 复用 OceanBase 连接基础 | 产品化细化故事 | P0 | `draft` |
| US-022 | `SEED-022` | 异步请求 Seed 公共能力 | 产品化细化故事 | P0 | `draft` |
| US-023 | `SEED-023` | 跨仓库升级可迁移和回滚 | 产品化细化故事 | P0 | `draft` |

> 状态：`draft` / `ready` / `done` / `deferred`。
> 子项目优先级继承 Maia 主项目：展开“上游来源”中的主 Story，取最高优先级（P0 > P1 > P2）；若主 Story 明示继承消费 Story，则保留该消费 Story 的既有优先级。

## US-001 — 有统一配置模型

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 服务开发者 |
| 使用上下文 / 前置条件 | 服务开发者在消费工程通过已接受 Assignment 请求通用能力并推进“有统一配置模型”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“有统一配置模型”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“文件/环境/Secret 引用可组合”与“启动失败指出字段”进入同一可复验闭环前，“可观察到：文件/环境/Secret 引用可组合；启动失败指出字段；输出摘要自动脱敏”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：文件/环境/Secret 引用可组合；启动失败指出字段；输出摘要自动脱敏。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-001`；上游来源 `FND-004/021`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为服务开发者，我希望有统一配置模型。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-004/021` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-001-AC-01 | 该 Story 所需业务前置条件成立 | 服务开发者发起“有统一配置模型”对应操作 | 文件/环境/Secret 引用可组合 |
| US-001-AC-02 | 该 Story 所需业务前置条件成立 | 服务开发者发起“有统一配置模型”对应操作 | 启动失败指出字段 |
| US-001-AC-03 | 该 Story 所需业务前置条件成立 | 服务开发者发起“有统一配置模型”对应操作 | 输出摘要自动脱敏 |

## US-002 — 传播身份与租户上下文

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 开发者 |
| 使用上下文 / 前置条件 | 开发者在消费工程通过已接受 Assignment 请求通用能力并推进“传播身份与租户上下文”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“传播身份与租户上下文”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“用户/服务/终端 principal 分离”与“HTTP/消息上下文可验证”进入同一可复验闭环前，“可观察到：用户/服务/终端 principal 分离；HTTP/消息上下文可验证；缺失租户默认拒绝”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：用户/服务/终端 principal 分离；HTTP/消息上下文可验证；缺失租户默认拒绝。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-002`；上游来源 `FND-001/003`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为开发者，我希望传播身份与租户上下文。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-001/003` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-002-AC-01 | 该 Story 所需业务前置条件成立 | 开发者发起“传播身份与租户上下文”对应操作 | 用户/服务/终端 principal 分离 |
| US-002-AC-02 | 该 Story 所需业务前置条件成立 | 开发者发起“传播身份与租户上下文”对应操作 | HTTP/消息上下文可验证 |
| US-002-AC-03 | 该 Story 所需业务前置条件成立 | 开发者发起“传播身份与租户上下文”对应操作 | 缺失租户默认拒绝 |

## US-003 — 统一理解错误

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 消费者 |
| 使用上下文 / 前置条件 | 消费者在消费工程通过已接受 Assignment 请求通用能力并推进“统一理解错误”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“统一理解错误”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“code/retryable/userMessage/recovery 稳定”与“底层异常不外泄”进入同一可复验闭环前，“可观察到：code/retryable/userMessage/recovery 稳定；底层异常不外泄；各传输映射一致”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：code/retryable/userMessage/recovery 稳定；底层异常不外泄；各传输映射一致。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-003`；上游来源 `FND-005`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。合并切片证据：[PR #1](https://github.com/xiajingan/maia-seed/pull/1)（merge `803d76270922`）与 [PR #2](https://github.com/xiajingan/maia-seed/pull/2)（merge `a97c8e56eea6`）交付 transport-neutral 错误与重试契约，但技术方案明确把传输映射留给消费者。 |

**用户故事**：作为消费者，我希望统一理解错误。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-005` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：PR #1、#2 虽已合并，但未覆盖“各传输映射一致”这一 AC，因此本 Story 保持 `draft`。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-003-AC-01 | 该 Story 所需业务前置条件成立 | 消费者发起“统一理解错误”对应操作 | code/retryable/userMessage/recovery 稳定 |
| US-003-AC-02 | 该 Story 所需业务前置条件成立 | 消费者发起“统一理解错误”对应操作 | 底层异常不外泄 |
| US-003-AC-03 | 该 Story 所需业务前置条件成立 | 消费者发起“统一理解错误”对应操作 | 各传输映射一致 |

## US-004 — 发布和消费版本化事件

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 服务开发者 |
| 使用上下文 / 前置条件 | 服务开发者在消费工程通过已接受 Assignment 请求通用能力并推进“发布和消费版本化事件”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“发布和消费版本化事件”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“envelope 字段完整”与“Schema 正反例和幂等夹具可复用”进入同一可复验闭环前，“可观察到：envelope 字段完整；Schema 正反例和幂等夹具可复用；破坏变更被 CI 拒绝”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：envelope 字段完整；Schema 正反例和幂等夹具可复用；破坏变更被 CI 拒绝。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-004`；上游来源 `FND-005/020`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为服务开发者，我希望发布和消费版本化事件。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-005/020` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-004-AC-01 | 该 Story 所需业务前置条件成立 | 服务开发者发起“发布和消费版本化事件”对应操作 | envelope 字段完整 |
| US-004-AC-02 | 该 Story 所需业务前置条件成立 | 服务开发者发起“发布和消费版本化事件”对应操作 | Schema 正反例和幂等夹具可复用 |
| US-004-AC-03 | 该 Story 所需业务前置条件成立 | 服务开发者发起“发布和消费版本化事件”对应操作 | 破坏变更被 CI 拒绝 |

## US-005 — 共享认证、加密、脱敏与重放防护端口

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 安全负责人 |
| 使用上下文 / 前置条件 | 安全负责人在消费工程通过已接受 Assignment 请求通用能力并推进“共享认证、加密、脱敏与重放防护端口”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“共享认证、加密、脱敏与重放防护端口”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“三类 principal 认证隔离”与“Provider 可替换”进入同一可复验闭环前，“可观察到：三类 principal 认证隔离；Provider 可替换；轮换可审计；日志和异常不含秘密”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：三类 principal 认证隔离；Provider 可替换；轮换可审计；日志和异常不含秘密。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-005`；上游来源 `FND-004/020`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。合并切片证据：[PR #2](https://github.com/xiajingan/maia-seed/pull/2)（merge `a97c8e56eea6`）与 [PR #3](https://github.com/xiajingan/maia-seed/pull/3)（merge `c9ce8e9eda4c`）交付 key ring、AEAD、轮换与 reference-keyed digest，但未交付三类 principal 的完整认证隔离。 |

**用户故事**：作为安全负责人，我希望共享认证、加密、脱敏与重放防护端口。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-004/020` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：PR #2、#3 虽已合并，但只覆盖本 Story 的加密、轮换和秘密不外泄切片，未完整覆盖认证隔离等全部 AC，因此保持 `draft`。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-005-AC-01 | 该 Story 所需业务前置条件成立 | 安全负责人发起“共享认证、加密、脱敏与重放防护端口”对应操作 | 三类 principal 认证隔离 |
| US-005-AC-02 | 该 Story 所需业务前置条件成立 | 安全负责人发起“共享认证、加密、脱敏与重放防护端口”对应操作 | Provider 可替换 |
| US-005-AC-03 | 该 Story 所需业务前置条件成立 | 安全负责人发起“共享认证、加密、脱敏与重放防护端口”对应操作 | 轮换可审计 |
| US-005-AC-04 | 该 Story 所需业务前置条件成立 | 安全负责人发起“共享认证、加密、脱敏与重放防护端口”对应操作 | 日志和异常不含秘密 |

## US-006 — 关键操作采用统一审计契约

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 审计人员 |
| 使用上下文 / 前置条件 | 审计人员在消费工程通过已接受 Assignment 请求通用能力并推进“关键操作采用统一审计契约”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“关键操作采用统一审计契约”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“主体/动作/目标/理由/前后摘要/结果/关联 ID 完整”与“风险失败策略可配置”进入同一可复验闭环前，“可观察到：主体/动作/目标/理由/前后摘要/结果/关联 ID 完整；风险失败策略可配置”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：主体/动作/目标/理由/前后摘要/结果/关联 ID 完整；风险失败策略可配置。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-006`；上游来源 `FND-003/005`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为审计人员，我希望关键操作采用统一审计契约。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-003/005` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-006-AC-01 | 该 Story 所需业务前置条件成立 | 审计人员发起“关键操作采用统一审计契约”对应操作 | 主体/动作/目标/理由/前后摘要/结果/关联 ID 完整 |
| US-006-AC-02 | 该 Story 所需业务前置条件成立 | 审计人员发起“关键操作采用统一审计契约”对应操作 | 风险失败策略可配置 |

## US-007 — 统一日志、指标和 Trace

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 运维人员 |
| 使用上下文 / 前置条件 | 运维人员在消费工程通过已接受 Assignment 请求通用能力并推进“统一日志、指标和 Trace”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“统一日志、指标和 Trace”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“关联 ID 自动注入”与“OTel 可跨 HTTP/消息传播”进入同一可复验闭环前，“可观察到：关联 ID 自动注入；OTel 可跨 HTTP/消息传播；敏感字段过滤”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：关联 ID 自动注入；OTel 可跨 HTTP/消息传播；敏感字段过滤。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-007`；上游来源 `FND-003/SOP-004`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为运维人员，我希望统一日志、指标和 Trace。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-003/SOP-004` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-007-AC-01 | 该 Story 所需业务前置条件成立 | 运维人员发起“统一日志、指标和 Trace”对应操作 | 关联 ID 自动注入 |
| US-007-AC-02 | 该 Story 所需业务前置条件成立 | 运维人员发起“统一日志、指标和 Trace”对应操作 | OTel 可跨 HTTP/消息传播 |
| US-007-AC-03 | 该 Story 所需业务前置条件成立 | 运维人员发起“统一日志、指标和 Trace”对应操作 | 敏感字段过滤 |

## US-008 — 提供健康探针和优雅停机

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 云原生服务 |
| 使用上下文 / 前置条件 | 云原生服务在消费工程通过已接受 Assignment 请求通用能力并推进“提供健康探针和优雅停机”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“提供健康探针和优雅停机”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“live/readiness 分离”与“SIGTERM 停止接流并 drain”进入同一可复验闭环前，“可观察到：live/readiness 分离；SIGTERM 停止接流并 drain；测试验证无请求丢失”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：live/readiness 分离；SIGTERM 停止接流并 drain；测试验证无请求丢失。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-008`；上游来源 `FND-021`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为云原生服务，我希望提供健康探针和优雅停机。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-021` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-008-AC-01 | 该 Story 所需业务前置条件成立 | 云原生服务发起“提供健康探针和优雅停机”对应操作 | live/readiness 分离 |
| US-008-AC-02 | 该 Story 所需业务前置条件成立 | 云原生服务发起“提供健康探针和优雅停机”对应操作 | SIGTERM 停止接流并 drain |
| US-008-AC-03 | 该 Story 所需业务前置条件成立 | 云原生服务发起“提供健康探针和优雅停机”对应操作 | 测试验证无请求丢失 |

## US-009 — 可靠调用其他服务

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 服务开发者 |
| 使用上下文 / 前置条件 | 服务开发者在消费工程通过已接受 Assignment 请求通用能力并推进“可靠调用其他服务”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“可靠调用其他服务”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“强制连接/请求超时、有限重试、幂等判定、熔断/半开恢复及追踪”与“不提供固定 IP 发现”进入同一可复验闭环前，“可观察到：强制连接/请求超时、有限重试、幂等判定、熔断/半开恢复及追踪；不提供固定 IP 发现”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：强制连接/请求超时、有限重试、幂等判定、熔断/半开恢复及追踪；不提供固定 IP 发现。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-009`；上游来源 `FND-020/021`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。合并切片证据：[PR #1](https://github.com/xiajingan/maia-seed/pull/1)（merge `803d76270922`）与 [PR #2](https://github.com/xiajingan/maia-seed/pull/2)（merge `a97c8e56eea6`）；accepted Response 将首个切片映射到 `SEED-009`，但交付范围仅为失败分类、重试 reference 与快照基础。 |

**用户故事**：作为服务开发者，我希望可靠调用其他服务。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-020/021` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：PR #1、#2 虽已合并，但未交付 HTTP 连接/请求超时、幂等判定、熔断/半开恢复及追踪的完整可靠调用客户端，因此本 Story 保持 `draft`。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-009-AC-01 | 该 Story 所需业务前置条件成立 | 服务开发者发起“可靠调用其他服务”对应操作 | 强制连接/请求超时、有限重试、幂等判定、熔断/半开恢复及追踪 |
| US-009-AC-02 | 该 Story 所需业务前置条件成立 | 服务开发者发起“可靠调用其他服务”对应操作 | 不提供固定 IP 发现 |

## US-010 — 安全发布 Seed wheel

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 发布负责人 |
| 使用上下文 / 前置条件 | 发布负责人在消费工程通过已接受 Assignment 请求通用能力并推进“安全发布 Seed wheel”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“安全发布 Seed wheel”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | PR #1 已发布并验证 `maia-seed==0.1.0` 的 Build Once wheel，PR #2 又以 `0.2.0` 完成同一发布链路及从 `0.1.0` 升级/回滚验证；两个 PR 均已合入 `develop`，最终制品、供应链证据和消费者锁定关系可追溯。 |
| 期望的可观察结果 | 可观察到：最终 version 的 wheel 只构建一次；签名/SBOM/provenance 与 SHA-256 固定；Test 验证的 digest 原样从 staged 提升 release；消费者锁定 version + hash。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-010`；上游来源 `FND-020/022`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；docs/deliveries/maia-seed.retry-contract.0.1.0.b1e5af73.json；docs/deliveries/maia-seed.retry-reference-foundation.0.2.0.b4467943.json；对应 delivery-verification receipts 与 API stability matrix。合并证据：[PR #1](https://github.com/xiajingan/maia-seed/pull/1)（merge `803d76270922`）与 [PR #2](https://github.com/xiajingan/maia-seed/pull/2)（merge `a97c8e56eea6`）。 |

**用户故事**：作为发布负责人，我希望安全发布 Seed wheel。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-020/022` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：无；PR #1、#2 的合并代码与不可变 Delivery/verification receipt 完整覆盖 Build Once、签名/SBOM/provenance、同 digest 提升及消费者 version+hash 锁定，支持状态 `done`。后续发布机制变化应新建 Story。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-010-AC-01 | 该 Story 所需业务前置条件成立 | 发布负责人发起“安全发布 Seed wheel”对应操作 | 最终 version 的 wheel 只构建一次 |
| US-010-AC-02 | 该 Story 所需业务前置条件成立 | 发布负责人发起“安全发布 Seed wheel”对应操作 | 签名/SBOM/provenance 与 SHA-256 固定 |
| US-010-AC-03 | 该 Story 所需业务前置条件成立 | 发布负责人发起“安全发布 Seed wheel”对应操作 | Test 验证的 digest 原样从 staged 提升 release |
| US-010-AC-04 | 该 Story 所需业务前置条件成立 | 发布负责人发起“安全发布 Seed wheel”对应操作 | 消费者锁定 version + hash |

## US-011 — Seed 保持无业务语义

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 架构负责人 |
| 使用上下文 / 前置条件 | 架构负责人在消费工程通过已接受 Assignment 请求通用能力并推进“Seed 保持无业务语义”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“Seed 保持无业务语义”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“依赖检查阻止业务工程和领域模型反向进入”与“K8s 能力限于应用运行契约”进入同一可复验闭环前，“可观察到：依赖检查阻止业务工程和领域模型反向进入；K8s 能力限于应用运行契约”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：依赖检查阻止业务工程和领域模型反向进入；K8s 能力限于应用运行契约。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-011`；上游来源 `ARCHITECTURE 3.2/6.1、FND-024`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为架构负责人，我希望 Seed 保持无业务语义。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `ARCHITECTURE 3.2/6.1、FND-024` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-011-AC-01 | 该 Story 所需业务前置条件成立 | 架构负责人发起“Seed 保持无业务语义”对应操作 | 依赖检查阻止业务工程和领域模型反向进入 |
| US-011-AC-02 | 该 Story 所需业务前置条件成立 | 架构负责人发起“Seed 保持无业务语义”对应操作 | K8s 能力限于应用运行契约 |

## US-012 — 用通用原语定义自己的状态机

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 领域工程 |
| 使用上下文 / 前置条件 | 领域工程在消费工程通过已接受 Assignment 请求通用能力并推进“用通用原语定义自己的状态机”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“用通用原语定义自己的状态机”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“合法/非法迁移、终态、并发版本和序列化可测”与“Seed 不包含 Task 等业务枚举”进入同一可复验闭环前，“可观察到：合法/非法迁移、终态、并发版本和序列化可测；Seed 不包含 Task 等业务枚举”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：合法/非法迁移、终态、并发版本和序列化可测；Seed 不包含 Task 等业务枚举。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-012`；上游来源 `FND-006/020`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为领域工程，我希望用通用原语定义自己的状态机。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-006/020` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-012-AC-01 | 该 Story 所需业务前置条件成立 | 领域工程发起“用通用原语定义自己的状态机”对应操作 | 合法/非法迁移、终态、并发版本和序列化可测 |
| US-012-AC-02 | 该 Story 所需业务前置条件成立 | 领域工程发起“用通用原语定义自己的状态机”对应操作 | Seed 不包含 Task 等业务枚举 |

## US-013 — 只在调用点解析通用 Secret

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 服务开发者 |
| 使用上下文 / 前置条件 | 服务开发者在消费工程通过已接受 Assignment 请求通用能力并推进“只在调用点解析通用 Secret”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“只在调用点解析通用 Secret”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“SecretProvider 支持文件/环境引用、失败可观测、值不可序列化或进入日志，生命周期结束可清理”进入同一可复验闭环前，“可观察到：SecretProvider 支持文件/环境引用、失败可观测、值不可序列化或进入日志，生命周期结束可清理”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：SecretProvider 支持文件/环境引用、失败可观测、值不可序列化或进入日志，生命周期结束可清理。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-013`；上游来源 `FND-004/021`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为服务开发者，我希望只在调用点解析通用 Secret。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-004/021` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-013-AC-01 | 该 Story 所需业务前置条件成立 | 服务开发者发起“只在调用点解析通用 Secret”对应操作 | SecretProvider 支持文件/环境引用、失败可观测、值不可序列化或进入日志，生命周期结束可清理 |

## US-014 — 关联上下文不会泄漏到错误异步任务

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 开发者 |
| 使用上下文 / 前置条件 | 开发者在消费工程通过已接受 Assignment 请求通用能力并推进“关联上下文不会泄漏到错误异步任务”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“关联上下文不会泄漏到错误异步任务”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“background task 显式序列化允许字段”与“contextvar 生命周期清理”进入同一可复验闭环前，“可观察到：background task 显式序列化允许字段；contextvar 生命周期清理；跨 tenant 并发测试无串扰”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：background task 显式序列化允许字段；contextvar 生命周期清理；跨 tenant 并发测试无串扰。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-014`；上游来源 `FND-003`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为开发者，我希望关联上下文不会泄漏到错误异步任务。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-003` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-014-AC-01 | 该 Story 所需业务前置条件成立 | 开发者发起“关联上下文不会泄漏到错误异步任务”对应操作 | background task 显式序列化允许字段 |
| US-014-AC-02 | 该 Story 所需业务前置条件成立 | 开发者发起“关联上下文不会泄漏到错误异步任务”对应操作 | contextvar 生命周期清理 |
| US-014-AC-03 | 该 Story 所需业务前置条件成立 | 开发者发起“关联上下文不会泄漏到错误异步任务”对应操作 | 跨 tenant 并发测试无串扰 |

## US-015 — 未知写结果进入对账而非盲重试

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 调用方 |
| 使用上下文 / 前置条件 | 调用方在消费工程通过已接受 Assignment 请求通用能力并推进“未知写结果进入对账而非盲重试”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“未知写结果进入对账而非盲重试”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“client 声明 idempotency/result query”与“deadline budget”进入同一可复验闭环前，“可观察到：client 声明 idempotency/result query；deadline budget；超时 disposition 明确；测试只产生一次效果”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：client 声明 idempotency/result query；deadline budget；超时 disposition 明确；测试只产生一次效果。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-015`；上游来源 `FND-005/020`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。合并切片证据：[PR #1](https://github.com/xiajingan/maia-seed/pull/1)（merge `803d76270922`）与 [PR #2](https://github.com/xiajingan/maia-seed/pull/2)（merge `a97c8e56eea6`）提供重试分类和不可变 reference snapshot，但不拥有消费者的写入对账语义。 |

**用户故事**：作为调用方，我希望未知写结果进入对账而非盲重试。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-005/020` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：PR #1、#2 虽已合并，但未完整覆盖 idempotency/result query、deadline、超时 disposition 与 once-only effect 全部 AC，因此本 Story 保持 `draft`。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-015-AC-01 | 该 Story 所需业务前置条件成立 | 调用方发起“未知写结果进入对账而非盲重试”对应操作 | client 声明 idempotency/result query |
| US-015-AC-02 | 该 Story 所需业务前置条件成立 | 调用方发起“未知写结果进入对账而非盲重试”对应操作 | deadline budget |
| US-015-AC-03 | 该 Story 所需业务前置条件成立 | 调用方发起“未知写结果进入对账而非盲重试”对应操作 | 超时 disposition 明确 |
| US-015-AC-04 | 该 Story 所需业务前置条件成立 | 调用方发起“未知写结果进入对账而非盲重试”对应操作 | 测试只产生一次效果 |

## US-016 — 依赖故障反映到正确探针

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 运维人员 |
| 使用上下文 / 前置条件 | 运维人员在消费工程通过已接受 Assignment 请求通用能力并推进“依赖故障反映到正确探针”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“依赖故障反映到正确探针”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“live 不因依赖短断失败”与“ready 按关键依赖/排空状态”进入同一可复验闭环前，“可观察到：live 不因依赖短断失败；ready 按关键依赖/排空状态；SIGTERM 先摘流后 drain”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：live 不因依赖短断失败；ready 按关键依赖/排空状态；SIGTERM 先摘流后 drain。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-016`；上游来源 `FND-021`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为运维人员，我希望依赖故障反映到正确探针。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-021` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-016-AC-01 | 该 Story 所需业务前置条件成立 | 运维人员发起“依赖故障反映到正确探针”对应操作 | live 不因依赖短断失败 |
| US-016-AC-02 | 该 Story 所需业务前置条件成立 | 运维人员发起“依赖故障反映到正确探针”对应操作 | ready 按关键依赖/排空状态 |
| US-016-AC-03 | 该 Story 所需业务前置条件成立 | 运维人员发起“依赖故障反映到正确探针”对应操作 | SIGTERM 先摘流后 drain |

## US-017 — 公开 API 变化可治理

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 维护者 |
| 使用上下文 / 前置条件 | 维护者在消费工程通过已接受 Assignment 请求通用能力并推进“公开 API 变化可治理”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“公开 API 变化可治理”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“stability matrix、deprecation warning、consumer range 和删除版本”与“无无限期 alias”进入同一可复验闭环前，“可观察到：stability matrix、deprecation warning、consumer range 和删除版本；无无限期 alias”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：stability matrix、deprecation warning、consumer range 和删除版本；无无限期 alias。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-017`；上游来源 `FND-020/024`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。合并切片证据：[PR #1](https://github.com/xiajingan/maia-seed/pull/1)（merge `803d76270922`）与 [PR #2](https://github.com/xiajingan/maia-seed/pull/2)（merge `a97c8e56eea6`）更新了 API stability matrix、消费者范围与兼容策略。 |

**用户故事**：作为维护者，我希望公开 API 变化可治理。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-020/024` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：PR #1、#2 虽已合并，但当前仅证明 additive 版本的治理；尚无弃用 warning 与按登记版本删除旧入口的完整代码闭环，因此本 Story 保持 `draft`。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-017-AC-01 | 该 Story 所需业务前置条件成立 | 维护者发起“公开 API 变化可治理”对应操作 | stability matrix、deprecation warning、consumer range 和删除版本 |
| US-017-AC-02 | 该 Story 所需业务前置条件成立 | 维护者发起“公开 API 变化可治理”对应操作 | 无无限期 alias |

## US-018 — 自动发现日志中的秘密

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 安全负责人 |
| 使用上下文 / 前置条件 | 安全负责人在消费工程通过已接受 Assignment 请求通用能力并推进“自动发现日志中的秘密”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“自动发现日志中的秘密”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“fixture 注入 canary”与“日志/trace/error/audit snapshot 扫描”进入同一可复验闭环前，“可观察到：fixture 注入 canary；日志/trace/error/audit snapshot 扫描；发现即 CI 失败”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：fixture 注入 canary；日志/trace/error/audit snapshot 扫描；发现即 CI 失败。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-018`；上游来源 `FND-004`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。合并切片证据：[PR #2](https://github.com/xiajingan/maia-seed/pull/2)（merge `a97c8e56eea6`）新增 canary 测试，[PR #3](https://github.com/xiajingan/maia-seed/pull/3)（merge `c9ce8e9eda4c`）扩展其对 digest 能力的覆盖。 |

**用户故事**：作为安全负责人，我希望自动发现日志中的秘密。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-004` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：PR #2、#3 虽已合并，但当前 canary 测试只覆盖日志、异常与 JSON/repr 片段，未完整证明 trace/error/audit 全部 snapshot 扫描，因此本 Story 保持 `draft`。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-018-AC-01 | 该 Story 所需业务前置条件成立 | 安全负责人发起“自动发现日志中的秘密”对应操作 | fixture 注入 canary |
| US-018-AC-02 | 该 Story 所需业务前置条件成立 | 安全负责人发起“自动发现日志中的秘密”对应操作 | 日志/trace/error/audit snapshot 扫描 |
| US-018-AC-03 | 该 Story 所需业务前置条件成立 | 安全负责人发起“自动发现日志中的秘密”对应操作 | 发现即 CI 失败 |

## US-019 — 用唯一无状态算法合并权限事实

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 资源服务开发者 |
| 使用上下文 / 前置条件 | 资源服务开发者在消费工程通过已接受 Assignment 请求通用能力并推进“用唯一无状态算法合并权限事实”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“用唯一无状态算法合并权限事实”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“`access-scope-kernel.v1` 输入 capability/effective Own 与本地 Creator/Owner/Us…”与“不持有策略或组图”进入同一可复验闭环前，“可观察到：`access-scope-kernel.v1` 输入 capability/effective Own 与本地 Creator/Owner/User，输出 s…”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：`access-scope-kernel.v1` 输入 capability/effective Own 与本地 Creator/Owner/User，输出 scope/来源；不持有策略或组图；Mud/Stem/Tea 运行相同正反夹具；破坏变化升 major 并有消费者迁移。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-019`；上游来源 `FND-007～011/018/020`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为资源服务开发者，我希望用唯一无状态算法合并权限事实。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-007～011/018/020` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-019-AC-01 | 该 Story 所需业务前置条件成立 | 资源服务开发者发起“用唯一无状态算法合并权限事实”对应操作 | `access-scope-kernel.v1` 输入 capability/effective Own 与本地 Creator/Owner/User，输出 scope/来源 |
| US-019-AC-02 | 该 Story 所需业务前置条件成立 | 资源服务开发者发起“用唯一无状态算法合并权限事实”对应操作 | 不持有策略或组图 |
| US-019-AC-03 | 该 Story 所需业务前置条件成立 | 资源服务开发者发起“用唯一无状态算法合并权限事实”对应操作 | Mud/Stem/Tea 运行相同正反夹具 |
| US-019-AC-04 | 该 Story 所需业务前置条件成立 | 资源服务开发者发起“用唯一无状态算法合并权限事实”对应操作 | 破坏变化升 major 并有消费者迁移 |

## US-020 — 复用可替换的 Redis 访问基础

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | Python 服务开发者 |
| 使用上下文 / 前置条件 | Python 服务开发者在消费工程通过已接受 Assignment 请求通用能力并推进“复用可替换的 Redis 访问基础”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“复用可替换的 Redis 访问基础”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“连接/关闭、健康检查、超时、序列化和观测可替换”与“业务工程自有 key/TTL/一致性语义”进入同一可复验闭环前，“可观察到：连接/关闭、健康检查、超时、序列化和观测可替换；业务工程自有 key/TTL/一致性语义；不形成全局 client 或业务缓存 API”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：连接/关闭、健康检查、超时、序列化和观测可替换；业务工程自有 key/TTL/一致性语义；不形成全局 client 或业务缓存 API。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-020`；上游来源 `消费工程 Story/FND-021/022`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为 Python 服务开发者，我希望复用可替换的 Redis 访问基础。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `消费工程 Story/FND-021/022` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-020-AC-01 | 该 Story 所需业务前置条件成立 | Python 服务开发者发起“复用可替换的 Redis 访问基础”对应操作 | 连接/关闭、健康检查、超时、序列化和观测可替换 |
| US-020-AC-02 | 该 Story 所需业务前置条件成立 | Python 服务开发者发起“复用可替换的 Redis 访问基础”对应操作 | 业务工程自有 key/TTL/一致性语义 |
| US-020-AC-03 | 该 Story 所需业务前置条件成立 | Python 服务开发者发起“复用可替换的 Redis 访问基础”对应操作 | 不形成全局 client 或业务缓存 API |

## US-021 — 复用 OceanBase 连接基础

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | Python 服务开发者 |
| 使用上下文 / 前置条件 | Python 服务开发者在消费工程通过已接受 Assignment 请求通用能力并推进“复用 OceanBase 连接基础”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“复用 OceanBase 连接基础”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“Engine/Session 生命周期、连接池、健康检查和方言探测可独立测试”与“业务 Model/Repository/SQL/Alembic 留在所有者工程”进入同一可复验闭环前，“可观察到：Engine/Session 生命周期、连接池、健康检查和方言探测可独立测试；业务 Model/Repository/SQL/Alembic 留在所有者工程”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：Engine/Session 生命周期、连接池、健康检查和方言探测可独立测试；业务 Model/Repository/SQL/Alembic 留在所有者工程。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-021`；上游来源 `消费工程 Story/FND-021/022`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为 Python 服务开发者，我希望复用 OceanBase 连接基础。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `消费工程 Story/FND-021/022` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-021-AC-01 | 该 Story 所需业务前置条件成立 | Python 服务开发者发起“复用 OceanBase 连接基础”对应操作 | Engine/Session 生命周期、连接池、健康检查和方言探测可独立测试 |
| US-021-AC-02 | 该 Story 所需业务前置条件成立 | Python 服务开发者发起“复用 OceanBase 连接基础”对应操作 | 业务 Model/Repository/SQL/Alembic 留在所有者工程 |

## US-022 — 异步请求 Seed 公共能力

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 依赖需求提出者 |
| 使用上下文 / 前置条件 | 依赖需求提出者在消费工程通过已接受 Assignment 请求通用能力并推进“异步请求 Seed 公共能力”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“异步请求 Seed 公共能力”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“`dependency` Assignment 记录来源 Story/Task、目标行为、无业务语义边界和验收”与“不预填未知版本、不启动 Seed Sprint”进入同一可复验闭环前，“可观察到：`dependency` Assignment 记录来源 Story/Task、目标行为、无业务语义边界和验收；不预填未知版本、不启动 Seed Sprint…”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：`dependency` Assignment 记录来源 Story/Task、目标行为、无业务语义边界和验收；不预填未知版本、不启动 Seed Sprint；Seed Response 必须自校验、绑定原 Assignment，接受时映射本地 Story。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-022`；上游来源 `FND-020/022/024`；迁移前优先级“继承 Assignment”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。 |

**用户故事**：作为依赖需求提出者，我希望异步请求 Seed 公共能力。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-020/022/024` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：本场景依据当前 Architecture、原始需求与已发现证据整理；未发现独立用户访谈。新增证据若改变角色、前置条件或困难，应显式修订 Story 后再提升状态。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-022-AC-01 | 该 Story 所需业务前置条件成立 | 依赖需求提出者发起“异步请求 Seed 公共能力”对应操作 | `dependency` Assignment 记录来源 Story/Task、目标行为、无业务语义边界和验收 |
| US-022-AC-02 | 该 Story 所需业务前置条件成立 | 依赖需求提出者发起“异步请求 Seed 公共能力”对应操作 | 不预填未知版本、不启动 Seed Sprint |
| US-022-AC-03 | 该 Story 所需业务前置条件成立 | 依赖需求提出者发起“异步请求 Seed 公共能力”对应操作 | Seed Response 必须自校验、绑定原 Assignment，接受时映射本地 Story |

## US-023 — 跨仓库升级可迁移和回滚

### 场景内核

| 字段 | 内容 |
|------|------|
| 使用者 | 发布负责人 |
| 使用上下文 / 前置条件 | 发布负责人在消费工程通过已接受 Assignment 请求通用能力并推进“跨仓库升级可迁移和回滚”时；前置条件是无业务语义边界、消费者验收、组织信任根和兼容要求可验证。 |
| 触发事件 | 当“跨仓库升级可迁移和回滚”相关的业务请求、状态变化或治理动作发生时。 |
| 当前做法与困难 | 当前只能由真实消费者 Assignment 驱动公共能力，Architecture 明确禁止脱离需求提前抽象；在“采用 add→migrate→remove”与“登记兼容矩阵、消费者、升级/回滚和旧入口删除版本”进入同一可复验闭环前，“可观察到：采用 add→migrate→remove；登记兼容矩阵、消费者、升级/回滚和旧入口删除版本；首次 Delivery 前配置组织真实信任根及签名/SBOM/pr…”仍缺少端到端证据。 |
| 期望的可观察结果 | 可观察到：采用 add→migrate→remove；登记兼容矩阵、消费者、升级/回滚和旧入口删除版本；首次 Delivery 前配置组织真实信任根及签名/SBOM/provenance verifier，空配置必须 fail closed；Test/Production 不用 Git/path 依赖。 |
| 来源 / 证据 | Maia 既有需求输入，旧 ID `SEED-023`；上游来源 `FND-020/024`；迁移前优先级“继承消费 Story”；场景证据：ARCHITECTURE.md §1–§6；未发现本 Story 的关联 PRD 或产品验收正文。合并切片证据：[PR #1](https://github.com/xiajingan/maia-seed/pull/1)（merge `803d76270922`）完成首次 adoption/rollback，[PR #2](https://github.com/xiajingan/maia-seed/pull/2)（merge `a97c8e56eea6`）完成 `0.1.0`→`0.2.0` 升级/回滚及供应链验证。 |

**用户故事**：作为发布负责人，我希望跨仓库升级可迁移和回滚。

### 约束与非目标

- 必须保持的业务规则：保持原故事的角色、范围、状态与授权语义；不得削弱下列行为验收。
- 外部约束：沿用上游来源 `FND-020/024` 及项目级策略。
- 非目标：不包含原故事及 AC 未声明的产品或技术扩展。
- 已知假设 / 待确认问题：PR #1、#2 虽已合并，但 add→migrate→remove 当前只完成 add/migrate 和 removal 计划，旧入口尚未按登记版本实际删除，因此本 Story 保持 `draft`。

### 行为验收示例

| AC ID | Given | When | Then |
|-------|-------|------|------|
| US-023-AC-01 | 该 Story 所需业务前置条件成立 | 发布负责人发起“跨仓库升级可迁移和回滚”对应操作 | 采用 add→migrate→remove |
| US-023-AC-02 | 该 Story 所需业务前置条件成立 | 发布负责人发起“跨仓库升级可迁移和回滚”对应操作 | 登记兼容矩阵、消费者、升级/回滚和旧入口删除版本 |
| US-023-AC-03 | 该 Story 所需业务前置条件成立 | 发布负责人发起“跨仓库升级可迁移和回滚”对应操作 | 首次 Delivery 前配置组织真实信任根及签名/SBOM/provenance verifier，空配置必须 fail closed |
| US-023-AC-04 | 该 Story 所需业务前置条件成立 | 发布负责人发起“跨仓库升级可迁移和回滚”对应操作 | Test/Production 不用 Git/path 依赖 |

## 场景证据与后续校准

- 本文件每条 Story 的使用上下文、前置条件、当前做法与困难，已按当前 ARCHITECTURE.md、原始需求及可定位的 PRD/验收证据逐项补全。
- Architecture 描述目标边界，不自动等同于已交付现状；没有关联 PRD/产品验收时应在来源中显式标明证据层级，但不以其单独判定 Story 状态。
- Story 状态以代码合并事实为准：只有已合并 PR 的代码范围完整覆盖该 Story 全部 AC 时才标记为 `done`；无 PR、PR 未合并、无法追溯到 Story 或仅交付部分切片时均保持 `draft`。
- 新的用户访谈、生产观察或验收证据若改变角色、触发、前置条件或主要困难，应显式修订 Story 与 AC，不在 Sprint 中隐式改写。
- 子项目优先级已按所引用 Maia 主 Story 的最高优先级继承；该规则表达业务紧迫度，不代表当前 Story 已达到 ready。

## Story 编写规范

1. **问题优先**：写清场景、触发、困难和结果；不得用拟采用的页面、接口、表或中间件替代问题描述。
2. **最小充分**：场景内核、约束与行为验收不可缺失；不要求虚构不存在的指标、痛点或异常。
3. **可观察验收**：每条 AC 有稳定 ID，描述外部行为和判定结果；不强制凑数量或填写执行命令。
4. **约束分层**：业务、合规与兼容约束写在 Story；技术基线写在 `ARCHITECTURE.md`。
5. **显式未知**：证据不足时记录假设或待确认问题，禁止把推测包装成事实。
6. **保持历史**：已完成 Story 的行为变更通过新 Story 或显式版本记录表达。
7. **规划后置**：Sprint 可按 AC 拆分，但必须保留 Story/AC 追溯。
8. **机器门禁**：Feature Sprint 的 `source_stories` 只能引用 `ready` 且字段完整的 Story/AC。
